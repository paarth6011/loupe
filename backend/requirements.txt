fastapi==0.136.3
uvicorn[standard]==0.49.0
pydantic==2.13.4
pydantic-settings==2.14.1
SQLAlchemy==2.0.50
alembic==1.18.4
psycopg[binary]==3.3.4
python-jose[cryptography]==3.5.0
passlib==1.7.4
# Pinned: passlib 1.7.4 is incompatible with bcrypt >= 4.1 (backend self-test crash).
bcrypt==4.0.1
python-multipart==0.0.32
redis==5.2.1
anthropic==0.109.1
# Used at runtime for the local-Ollama summarizer (and by the test client).
httpx==0.28.1

# Tests
pytest==8.3.4
